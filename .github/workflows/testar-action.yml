name: Executar minha action
on:
  push:
    branches:
      - main
jobs:
  python-action:
    runs-on: ubuntu-latest
    steps:

      - name: Usar a ação personalizada test
        id: colaborador
        uses: lccinacio/action-python/actions@v27.0
        with:
          nome: 'Lucas'
          sobrenome: 'Inacio'
          cargo: 'Engenheiro de qualidade'